components:
  schemas:
    Notification:
      allOf:
        - $ref: "../common.yaml#/components/schemas/Id"
        - type: object
          properties:
            quote_id:
              type: integer
              example: 1
            sender_id:
              type: integer
              example: 1
            receiver_id:
              type: integer
              example: 1
            commented:
              type: integer
              default: 0
            liked:
              type: integer
              default: 0
            read:
              type: integer
            default: 0
        - $ref: "../common.yaml#/components/schemas/TimeStamps"
